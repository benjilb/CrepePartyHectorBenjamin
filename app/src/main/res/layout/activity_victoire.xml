<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.example.crepeparty_hector_benjamin.StaticRoadView
        android:id="@+id/road"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="24dp">

        <!-- Coupe au-dessus du titre -->
        <ImageView
            android:id="@+id/imgCup"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/cup"
            android:contentDescription="@null"
            android:importantForAccessibility="no" />


        <!-- Titre avec police pixelisé -->
        <TextView
            android:id="@+id/textVictoire"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Vous avez gagnez felicitations"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:fontFamily="@font/pixel"
            android:layout_marginBottom="24dp"/>

        <!-- Voiture sélectionnée par l’utilisateur -->
        <ImageView
            android:id="@+id/imgCar"
            android:layout_width="96dp"
            android:layout_height="144dp"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:layout_marginBottom="24dp"
            android:contentDescription="@null"
            android:importantForAccessibility="no"/>
        <Button
            android:id="@+id/buttonRetour"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Retour" />
    </LinearLayout>

</FrameLayout>
